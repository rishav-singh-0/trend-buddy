[project]
name = "trend-buddy-shared"
version = "0.1.0"
description = "Shared Python contracts, clients and config helpers for Trend Buddy."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/trend_buddy_shared"]
