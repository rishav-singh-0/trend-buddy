
## Technology Stack

- Go (version `1.26.1`) for microservices and apis
- Vue js for frontend
- For Python (version `3.9`) strictly use `uv` package manager
- Any python module should be run using uv (`uv run xyz`)
- Use `make` for building or running docker
- All commands should be run inside Docker environment
