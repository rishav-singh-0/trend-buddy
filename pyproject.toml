[project]
name = "trend-buddy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.128.8",
    "uvicorn>=0.39.0",
    "websockets>=15.0.1",
    "yfinance>=0.2.66",
]

[project.scripts]
trend-buddy = "main:main"
